; tests writing of basic smtlib output


(def F [z^2 + y^2 + x^2 < 1/2 /\ z + y + x < 1/3])

(smtlib-store F "/tmp/tst.smt2")

(def G (smtlib-load "/tmp/tst.smt2"))

(def res (if (equal? F G) "pass" "fail"))
(display res " : test smt07\n")
