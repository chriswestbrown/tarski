To compile tarski:

0. Set the saclib environment variable to the saclib2.2 directory (IE export saclib=/home/mydir/saclib2.2)
1. Set the qe environment variable to the qesource directory (IE export qe=/home/mydir/qesource )
2. Set the TMROOT environment variable to the minisat directory (IE export TMROOT=home/mydir/minisat)
3. Add $qe/bin to your PATH variable (so that tarski can find the QEPCAD binary)
4. Run tarInstall.sh